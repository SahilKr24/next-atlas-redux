const MainContent = ({ children }) => {
  return (
    <div
      style={{
        marginLeft: 270,
        padding: 40,
      }}
    >
      {children}
    </div>
  );
};

export default MainContent;
